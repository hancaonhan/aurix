node_modules/

# Bí mật — không bao giờ đưa lên kho mã
.env
.env.*
!.env.example

# Dữ liệu chạy thật
server/data/*.db
server/data/*.db-wal
server/data/*.db-shm
server/data/backups/

# Tạm
*.log
.DS_Store
